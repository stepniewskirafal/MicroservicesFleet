# Parallel module builds are opt-in via CLI: mvn -T 1C <goal>
# Do NOT set -T here — PITest and some plugins cannot parse the core-multiplier syntax (e.g. '1C'),
# which causes: "Invalid threads core multiplier value: ' 1C'"
