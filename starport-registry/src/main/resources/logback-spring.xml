<?xml version="1.0" encoding="UTF-8"?>
<!--
  JSON console logging (logstash-logback-encoder).
  Output is consumed by Grafana Alloy → Loki and sampled per ADR-0034:
    ERROR / WARN / FATAL  → 100%
    INFO  / DEBUG / TRACE → 10%
  Test profile keeps Spring Boot's default text pattern for readable test output.
-->
<configuration scan="false">
    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="unknown"/>

    <springProfile name="!test">
        <appender name="JSON_STDOUT" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <!-- Suppress Logback context map (which would re-emit `appName` from springProperty) -->
                <includeContext>false</includeContext>
                <customFields>{"service":"${appName}"}</customFields>
                <fieldNames>
                    <timestamp>@timestamp</timestamp>
                    <message>message</message>
                    <logger>logger</logger>
                    <thread>thread</thread>
                    <levelValue>[ignore]</levelValue>
                </fieldNames>
                <includeMdcKeyName>traceId</includeMdcKeyName>
                <includeMdcKeyName>spanId</includeMdcKeyName>
                <includeMdcKeyName>reservationId</includeMdcKeyName>
                <includeMdcKeyName>routeId</includeMdcKeyName>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON_STDOUT"/>
        </root>
    </springProfile>

    <springProfile name="test">
        <include resource="org/springframework/boot/logging/logback/base.xml"/>
    </springProfile>
</configuration>
